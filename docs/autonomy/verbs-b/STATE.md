# verbs-b — journal

The loop's journal for `docs/autonomy/verbs-b/QUEUE.md`: one entry per item,
newest at the bottom, in the shape `docs/autonomy/LOOP.md` prescribes.

## Opened 2026-08-30

The A9.1 real-model evaluation counted 136 verbs against 347 excluded routes,
47 of which excuse themselves with "a later intent set". This track turns its
share of those into verbs — or into exclusions that say honestly why an agent
must never do them.

## Parked 2026-08-30 — the owner closed the agent programme

No iteration ever ran here. The agents themselves are done: sixteen
product agents plus Ask alo, on intents, verified against a real model
(`docs/autonomy/agents/STATE.md`, A9.1), with the three defects that run
found fixed in wave A10. What this track would have closed is the
coverage gap that evaluation measured — the queue keeps the exact routes
and the rule for deciding each one, so it can be reopened without being
rewritten.

LOOP COMPLETE (parked, never started)

